<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head', {title: 'Editando: ' + product.name}) %>
<body>
	<!-- Header -->
	<%- include('partials/headerNavbar') %>
	<!-- /Header -->

	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Estás editando: <i>P<%= product.name %> </i></h2>
			</div>
		</div>
		<div class="col-12"><!-- el formulario viaja a update, que es donde esta el controlador que edita, el cual viaja por Put -->
			<form action="/products/update/<%= product.id %>?_method=PUT " enctype="multipart/form-data" method="POST" ><!-- formulARIO QUE EDITA ALGO YA EXISTENTE, VA POR put -->
				<div class="row product-detail">
					<div class="col-12 col-md-6">
						<label for="name" class="form-label">Nombre del producto:</label>
						<input type="text" id="name" name="name" placeholder="Ej: Zapatillas Reebok" class="form-input" value="<%= product.name %>">
					</div>
					<div class="col-12 col-md-6">
						<label for="price" class="form-label">Precio del producto:</label>
						<input type="number" id="price" name="price" placeholder="Ej: 5678" class="form-input" value="<%= product.price %>">
					</div>
					<div class="col-12 col-md-6">
						<label for="discount" class="form-label">Descuento:</label>
						<input type="number" id="discount" name="discount" placeholder="Ej: 15" class="form-input" value="<%= product.discount %>">
					</div>
					<div class="col-12 col-md-6">
						<label for="category" class="form-label">Categoría:</label>
						<select name="category" id="category" class="form-input">
							<option value="">Elegí</option>
							<option value="in-sale" <%= product.category === "in-sale" && 'selected' %> >En oferta</option>
							<option value="visited" <%= product.category === "visited" && 'selected' %>>Últimos agregados</option>
						</select>
					</div>
					<div class="col-8">
						<label for="description" class="form-label">Descipción:</label>
						<textarea name="description" id="description" class="form-input"></textarea>
					</div>
					<div class="col-4">
						<label for="imagen" class="form-label">imagen:</label>
						<input type="file" name="imagen" id="imagen" class="form-input" accept="image/*"></input>
					</div>
					<div class="col-12">
						<button type="submit" class="buy-now-button">Guardar producto</button> <!-- viaja a update, por metodo PUT -->
					</div>
				</div>
			</form>
		</div>

	</div>
	<!-- /todos los productos -->

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>	
</html>